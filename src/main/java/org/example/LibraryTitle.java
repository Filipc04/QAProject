package org.example;

public class LibraryTitle {
    /* Represents a unique library title (e.g., a book).

Attributes: isbn (int), title (String), author (String), totalCopies (int), availableCopies (int).

Methods: Getters and setters, isAvailable(). */
}
