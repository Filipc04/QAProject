package org.example;

public class LibrarySystem {
    /* Main class that ties everything together.

Attributes: libraryTitles (List<LibraryTitle>), members (List<Member>), librarian (Librarian).

Methods: addLibraryTitle(), removeLibraryTitle(), searchLibraryTitle(), notifyLibrarian(). */
}
