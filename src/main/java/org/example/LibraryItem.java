package org.example;

public class LibraryItem {
 /* Represents a physical copy of a LibraryTitle.

Attributes: itemId (int), libraryTitle (LibraryTitle), isBorrowed (boolean).

Methods: Getters and setters. */
}
