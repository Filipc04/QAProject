package org.example;

public class Librarian {
    /* Represents the librarian who manages the system.

Attributes: librarianId (int), name (String).

Methods: registerMember(), deleteMember(), suspendMember(), lendItem(), returnItem(). */
}
